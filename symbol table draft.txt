final no ch six, up to ch 7



//arbitrary values
const int rINT = 0, r//what other types are there

class storeSymbol{
	string name;
	int memaddr;
	int type;//?
//initialize class
storeSymbol(string nm, int memr, int ty);
};

storeSymbol::storeSymbol(string nm, int memr, int ty)
{
	name = nm;
	memaddr = memr;
	type = ty;
}

vector<storeSymbol> symtabl;

//table can be global so axe the reference
int symCheck(string symb, bool initl, int type, etc)
{
if (initl)
 {
 for (int i = 0; i < symtabl.size();i++)
  {
   if (symtabl[i].name == symb)
    { return -1;}
  }
 storeSymbol bep(symb,b,type);
 return bep.memaddr;


//else:
//if not in return error
int bap = -2;
for (thing in symtabl)
 {
  if (thing == symb.name)
   {bap = index;}
 }
return bap;
}