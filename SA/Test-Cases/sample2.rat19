
function add (one int, two int)
{
           return one + two;
}
function subtract (one int, two int)
{
           return one - two;
}
function gt (one int, two int)
{
while (low  <=  high )
    {  put (low);
        put (convert1x (low));
        low =  low  +  step;
    }
}
%%
int n, m, result;
get(n, m);
result = subtract(n, m);
%%
